<header class="c-header">
  {{ render '@logo-dmptool' }}
  {{> @header-menu-group }}
  {{ render '@nav-toggle' }}
  {{> @header-nav }}
  {{#if logged_in}}
    {{> @subheader }}
  {{/if}}
  {{#if notifications}}
    {{ render '@notification-group' }}
  {{/if}}
</header>
