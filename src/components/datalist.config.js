module.exports = {
  context: {
    datalist: {
      id: 'datalist-id'
    }
  },
  notes: "This is a `<datalist>`, which doesn't render anything on its own. See the [Login Sequence Step E](/components/detail/login--e) where it's used with the **Institution** text field."
}
